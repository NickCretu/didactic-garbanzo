---
import LanguageSwitcher from './LanguageSwitcher.astro';
import { useTranslations, getLocalizedPath, type Language } from '@i18n/index';

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

const base = import.meta.env.BASE_URL || '/';
const baseClean = base.endsWith('/') ? base.slice(0, -1) : base;
const homePath = `${baseClean}/${lang}/`;

// Use anchor links for single-page sections
const navItems = [
  { href: homePath, label: t.nav.home },
  { href: `${homePath}#catalog`, label: t.nav.catalog },
  { href: `${homePath}#shipping`, label: t.nav.shipping },
  { href: `${homePath}#about`, label: t.nav.about },
  { href: `${homePath}#faq`, label: t.nav.faq },
];
---

<header class="sticky top-0 z-[10000] bg-white/80 backdrop-blur-lg border-b border-gray-100">
  <nav class="container flex items-center justify-between h-16 md:h-18">
    <!-- Logo -->
    <a href={getLocalizedPath('/', lang)} class="flex items-center gap-2 text-xl font-semibold text-primary">
      <svg class="w-8 h-8" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect width="32" height="32" rx="8" fill="#0066CC"/>
        <path d="M10 16C10 12.6863 12.6863 10 16 10C19.3137 10 22 12.6863 22 16C22 19.3137 19.3137 22 16 22" stroke="white" stroke-width="2.5" stroke-linecap="round"/>
        <circle cx="16" cy="16" r="3" fill="white"/>
      </svg>
      <span class="hidden sm:inline">Pouch</span>
    </a>
    
    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-8">
      {navItems.map((item) => (
        <a
          href={item.href}
          class="text-body text-muted hover:text-primary transition-colors"
        >
          {item.label}
        </a>
      ))}
    </div>
    
    <!-- Right Side -->
    <div class="flex items-center gap-4">
      <LanguageSwitcher lang={lang} />
      
      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 -mr-2 text-muted hover:text-primary"
        aria-label="Toggle menu"
        aria-expanded="false"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path class="menu-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          <path class="close-icon hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
  </nav>
  
  <!-- Mobile Navigation Overlay -->
  <div id="mobile-menu" class="mobile-menu-overlay hidden">
    <div class="mobile-menu-content">
      <nav class="flex flex-col">
        {navItems.map((item) => (
          <a
            href={item.href}
            class="mobile-nav-link"
          >
            {item.label}
          </a>
        ))}
      </nav>
      <!-- Tap to close area -->
      <button id="mobile-menu-close-area" class="flex-1 w-full flex items-start justify-center pt-8" aria-label="Close menu">
        <span class="text-sm text-muted flex items-center gap-2">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
          {lang === 'ru' ? 'Нажмите, чтобы закрыть' : lang === 'ro' ? 'Apăsați pentru a închide' : 'Tap to close'}
        </span>
      </button>
    </div>
  </div>
</header>

<style>
  .mobile-menu-overlay {
    position: fixed;
    inset: 0;
    top: 64px;
    z-index: 9999;
    background: #ffffff !important;
    backdrop-filter: none;
  }
  
  .mobile-menu-overlay.hidden {
    display: none;
  }
  
  .mobile-menu-content {
    height: 100%;
    display: flex;
    flex-direction: column;
    background: #ffffff;
  }
  
  .mobile-nav-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    padding: 1.25rem 2rem;
    font-size: 1.25rem;
    font-weight: 500;
    color: #1a1a1a;
    transition: all 0.2s;
    border-bottom: 1px solid #f3f4f6;
    background: #ffffff;
  }
  
  .mobile-nav-link:first-child {
    border-top: 1px solid #f3f4f6;
  }
  
  .mobile-nav-link:hover,
  .mobile-nav-link:active {
    background: #f9fafb;
    color: #0066CC;
  }
</style>

<script>
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = menuBtn?.querySelector('.menu-icon');
  const closeIcon = menuBtn?.querySelector('.close-icon');
  const closeArea = document.getElementById('mobile-menu-close-area');
  
  function closeMenu() {
    mobileMenu?.classList.add('hidden');
    menuIcon?.classList.remove('hidden');
    closeIcon?.classList.add('hidden');
    menuBtn?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }
  
  menuBtn?.addEventListener('click', () => {
    const isOpen = mobileMenu?.classList.toggle('hidden');
    menuBtn.setAttribute('aria-expanded', (!isOpen).toString());
    menuIcon?.classList.toggle('hidden');
    closeIcon?.classList.toggle('hidden');
    
    // Prevent body scroll when menu is open
    document.body.style.overflow = isOpen ? '' : 'hidden';
  });
  
  // Close menu when clicking a link
  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeMenu);
  });
  
  // Close menu when clicking the close area
  closeArea?.addEventListener('click', closeMenu);
</script>
